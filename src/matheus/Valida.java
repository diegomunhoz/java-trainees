package matheus;

/**
 * Classe responsável por validar os dados inputados pelo usuário
 * @author mtomazs
 * @since 25/02/2021
 */
public class Valida {
		
		// verificando se o valor informado é maior que zero
		public static boolean isIntPositivo(int args) {
			return (args > 0);
		}
}
